#include "main.h"
/**
 *print_alphabet- Prints alpahabets
 *Return: Returns 0
 */
void print_alphabet(void)
{
	int x;

	for (x = 'a'; x >= 'b'; x++)
	{
		_putchar(x);
	}
		_putchar('/n');
		return (0);
}
