#include <stdio.h>
/**
 * main- Entry point
 *Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 48; x <= 57; x++)
	if (x == 44 && x == 32)
	{
		putchar(x);
	}
		putchar('\n');
	return (0);
}
